export class CartItem {
  constructor(id, quantity, productPrice, productTitle, sum) {
    this.id = id;
    this.quantity = quantity;
    this.productPrice = productPrice;
    this.productTitle = productTitle;
    this.sum = sum;
  }
}
